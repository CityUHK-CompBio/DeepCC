% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FunctionalSpectra.R
\name{getFunctionalSpectra}
\alias{getFunctionalSpectra}
\title{Generate Functional Spectra}
\usage{
getFunctionalSpectra(eps, geneSets = "MSigDBv7", scale = TRUE, cores = NULL)
}
\arguments{
\item{eps}{a data.frame containing gene expression profiles (each row presents one sample)}

\item{geneSets}{a List containing gene sets (default: MSigDB v7)}

\item{scale}{logical indicating whether to center each gene column (default: TRUE)}

\item{cores}{integer or NULL; number of CPU cores. NULL uses the native kernel's
thread pool. Set to a specific integer for legacy parallel fallback.}
}
\value{
a data.frame containing functional spectra
}
\description{
This function generates functional spectra for given gene expression profiles.
Uses a fast batch native kernel by default, with automatic fallback to the
legacy per-row parallel path when the input structure prevents batch indexing.
}
\examples{
\dontrun{
set.seed(42)
eps <- as.data.frame(matrix(rnorm(10*100), nrow=10, ncol=100))
colnames(eps) <- paste0("G", seq_len(100))
fs <- getFunctionalSpectra(eps, geneSets=list(setA=c("G1","G5","G20")))
}
}
\seealso{
\code{\link{getFunctionalSpectrum}} for a single expression profile.
}
