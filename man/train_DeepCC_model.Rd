% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DeepLearning.R
\name{train_DeepCC_model}
\alias{train_DeepCC_model}
\title{Train DeepCC Model}
\usage{
train_DeepCC_model(
  trainData,
  trainLabels,
  epochs = 100,
  dropout = 0.4,
  activation_func = "selu",
  validation_split = 0.2
)
}
\arguments{
\item{trainData}{a data.frame containing functional spectra of training data (each row presents one sample)}

\item{trainLabels}{a character vector containing lables of training data}

\item{epochs}{the number of epochs}

\item{dropout}{dropout rate}

\item{activation_func}{activation funtion}

\item{validation_split}{fraction of training data to use for validation}
}
\value{
a trained DeepCC model with \code{classifier}, \code{levels}, and
  \code{feature_names} fields
}
\description{
This function trains DeepCC Model on the training data using the modern
\code{keras3} interface. The network architecture, training recipe, and
legacy model format are preserved from DeepCC 0.1.1.
}
\examples{
\dontrun{
set.seed(42)
eps <- as.data.frame(matrix(rnorm(20*50), nrow=20, ncol=50))
colnames(eps) <- paste0("F", seq_len(50))
labels <- sample(c("A", "B", "C"), 20, replace=TRUE)
deepcc_model <- train_DeepCC_model(eps, labels, epochs=2)
}
}
