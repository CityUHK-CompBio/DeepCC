# Save DeepCC Model

Save DeepCC Model

## Usage

``` r
save_DeepCC_model(deepcc_model, prefix)
```

## Arguments

- deepcc_model:

  a trained DeepCC model

- prefix:

  file path prefix; outputs `prefix.hdf5` and `prefix.RData`
